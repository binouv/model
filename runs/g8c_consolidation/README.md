# G8C — continuation from real released G5S model/optimizer/RNG bytes

Registered, not yet a completed result. Six finite1000-update continuations, three ancestral seeds, trace-parent versus final-only-parent, common final-only consolidation. 469648parameters per model, NOT new300-800M training or Qwen parity.

G5S CPU replay run36229995214 completed and released real checkpoints; this stage restores them and DOES NOT repeat parent training. G6S/G7S textual plans have no recoverable completion in the inspected repo. G8C is a separate registered namespace, not a claim that those studies were completed.

Original G5S train regenerated byte-exact;1600new held questions,1400canonical groups, disjoint from every available parent training/validation/test group. Same stage2 minibatches/updates/parameters, but parent trace training had more token/FLOP cost. Fixed final endpoint, prompt-only unrestricted generation, EOS required.

Transported source gzip fragments are lossless copies of locally tested source, not executable opaque dependencies: bootstrap verifies SHA256, decompresses exactly three named files, syntax-checks, runs12tests, then publishes plaintext src/tests before training. Original model/data are exact existing Git blobs. The immutable parent receipt is checked before loading the user's own native checkpoint files. No external teacher/code execution or paid service.

CI commits only to flygraph/g8c-consolidation-20260926; no force or main overwrite. A new draft prerelease receives model+optimizer/RNG checkpoints every250updates, and actual files are downloaded back and SHA256-verified. It becomes public only after all six endpoints and full evaluation. Partial checkpoints are never a completed experiment. See NEXT_STATE and final release receipt.
