# FlyGraph Git import index

- `FLYGRAPH_TEXT_HISTORY_R3_R38.txt` — canonical text bundle of available reports, hypotheses, scripts, configs, tests, metrics, and manifests from R3–R38.
- `FLYGRAPH_SMALL_MODELS_BASE64.json` — lossless base64 copies of small verifier/model artifacts plus manifests for larger checkpoints.
- Historical R23–R26 deep-hop absolute metrics are invalidated by the benchmark overwrite bug; see the correction notice inside the text bundle.
- Active cognition line: protected/discrete address state → multi-hypothesis memory → consistency verifier → adaptive compute → off-policy residual experiments.
- R38 remains mixed/refuted on the primary criterion; 100M/200M/300M scaling is still deferred until 128-hop ranking stabilizes.